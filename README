The Scheme programming Language

# how to run the .scm program
If you have mit-scheme installed, then type "mit-scheme < test.scm" or "scheme < test.scm" in terminal to run test.scm.
If not, you can install mit-scheme first then do the above.

# the exercises naming convention
The program name for Exercise 2.4.3 would be ex2_4_3.scm. All programs' names for corresponding exercises' names follow this convention.
